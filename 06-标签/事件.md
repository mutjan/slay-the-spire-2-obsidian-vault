# 事件

```dataview
TABLE
  choice(contains(file.path, "02-卡牌"), "卡牌",
    choice(contains(file.path, "03-遗物"), "遗物",
      choice(contains(file.path, "04-药水"), "药水", "其他"))) as "类型",
  character as "角色"
FROM ""
WHERE rarity = [[事件]]
SORT type ASC, file.name ASC
```
